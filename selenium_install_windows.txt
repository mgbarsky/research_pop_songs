1. Open cmd as Administrator (right-click)
python -m pip install selenium

2. Download chrome webdriver from:
https://chromedriver.storage.googleapis.com/index.html?path=2.41/

place it in the PATH

3. If working with PyCharm - add package to the project:
File->Settings->Project interpreter